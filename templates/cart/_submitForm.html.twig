{{ form_start(submit_form,{id:"cartsubmit_form",action: path("app_order")}) }}
{{ form_row(submit_form.destination) }}
<fieldset id="infosPersonnellesLivraison" class="d-none">
	<legend>Renseignez vos informations personnelles</legend>
	{{ form_row(submit_form.email) }}
	{{ form_row(submit_form.nom) }}
	{{ form_row(submit_form.prenom) }}
	{{ form_row(submit_form.adresse) }}
	{{ form_row(submit_form.codePostal) }}
	{{ form_row(submit_form.ville) }}
	{{ form_row(submit_form.pays) }}
	{{ form_row(submit_form.submit_cart) }}


</fieldset>
{{ form_end(submit_form) }}
