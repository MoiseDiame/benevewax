<div class="container text-center my-5">
	<form action="{{path ('app_paytech')}}" method="post">
		<div class="my-2 row justify-content-center">
			<div class="col-md-6 col-lg-4">
				<input type="email" class="form-control" name="customer_email" id="" required="true" placeholder="Votre e-mail"/>
			</div>
		</div>
		<div class="mt-3">
			<button type="submit" class="rounded-pill btn btn-warning">Payer
				{{(total_to_pay / 100)|number_format(2, ',', '.')}}€</button>
		</div>
	</form>
</div>
